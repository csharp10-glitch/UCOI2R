package mazeRunner;

public class MazeRunner {
	public static void main(String[] args) {
		//initialize pilot
		//initialize inertial navigation system
		//confirm on starting square (yellow tape)
		//move forward 1 square (1') 
		// check for blue ball (set on interrupt)
		// check for red finish square (set on interrupt) if blue ball not collected, note location, resume search
		// look left, right, and center
		// if d > 1 square mark path as open, else path closed
		// choose square to move to via open path
		
		
	}
}
